<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main_search"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/blueBackground"
    tools:context="io.github.httpdhruvaalam.checkyourbias.UI.MainSearchActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/logo_big"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/main_logo"
        android:layout_margin="0dp"
        android:padding="0dp"
        android:adjustViewBounds="true"
        android:maxHeight="300dp"
        android:scaleType="fitXY" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/main_logo"
        android:id="@+id/search_bar_full"
        android:layout_centerInParent="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp">
        <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textPersonName"
            android:ems="10"
            android:id="@+id/search_bar_editText_main"
            android:hint="@string/search_bar_text_hint"
            android:layout_alignParentStart="true"
            android:layout_alignStart="@+id/search_bar_main_button"
            android:maxLines="1"
            android:background="@drawable/edit_text_border"
            android:padding="7dp"
            android:layout_marginEnd="10dp"
            />
        <Button
            android:id="@+id/search_bar_main_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/search"
            android:textColor="@color/white"
            android:background="@color/darkBlue"
            android:padding="-10dp"
            />
    </LinearLayout>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxWidth="280dp"
        app:srcCompat="@drawable/blurb"
        android:layout_below="@+id/search_bar_full"
        android:layout_centerHorizontal="true"
        android:id="@+id/imageView2"
        android:adjustViewBounds="true" />


</RelativeLayout>
